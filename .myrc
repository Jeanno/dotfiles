export EDITOR=vim
set -o vi
bindkey -v

viract() {
    if [ -z "$1" ]; then
        ls ~/env/;
        return
    fi
    source ~/env/$1/bin/activate
}

alias gpp="git pull && git push"

export PATH=$PATH:$DOTFILE_DIR/bin

